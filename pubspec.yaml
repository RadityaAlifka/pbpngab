name: nyobadoang
description: "A new Flutter project."
publish_to: 'none' # Change this if you plan to publish to pub.dev
version: 0.1.0+1 # Added build number

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.2.3
  percent_indicator: ^4.2.2
  shared_preferences: ^2.0.15 
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Add assets if needed, uncomment the line below if you plan to use assets
  # assets:
  #   - assets/
